#include<stdio.h>
#include<math.h>
int main()
{
    int n,c,s,r,k;
    printf("Enter the number: ");
    scanf("%d",&n);
    int dup=n;
    while (n!=0)
    {
        n=n/10;
        c++;
    }
}